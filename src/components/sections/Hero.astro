<section id="inicio" class="relative min-h-screen w-full overflow-hidden bg-pink-100">
  <!-- Video de fondo (lado izquierdo) -->
  <div class="absolute inset-0 w-1/2 hidden lg:block">
    <video
      class="object-cover w-full h-full"
      autoplay
      loop
      muted
      playsinline
    >
      <source src="/videos/hero.webm" type="video/mp4" />
    </video>
    <div class="absolute inset-0 bg-gradient-to-r from-black/30 to-black/10"></div>
  </div>

  <!-- Área de contenido derecha -->
  <div class="absolute top-0 right-0 w-full lg:w-[55%] h-full bg-pink-100">
    <div class="relative h-full flex items-center justify-center lg:justify-start px-4 lg:pl-16 lg:pr-24">
      <div class="w-full max-w-lg">
        <!-- Badge superior -->
        <div class="mb-8">
          <span class="inline-block px-4 py-2 rounded-full bg-white text-primary-600 text-sm font-medium">
            Psicóloga clínica
          </span>
        </div>

        <h1 class="text-6xl md:text-7xl tracking-tight mb-6 leading-tight text-primary-900 font-semibold">
          Viviana Poveda
        </h1>
        
        <p class="text-base md:text-lg text-gray-600 mb-8 leading-relaxed font-normal">
          Psicóloga clínica especializada en acompañamiento emocional desde un enfoque integrativo
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4">
          <a 
            href="#contacto"
            class="inline-flex items-center px-6 py-4 bg-primary-600 text-white font-semibold rounded-full hover:bg-primary-700 transition-colors duration-300 whitespace-nowrap"
          >
            Agenda tu consulta
            <svg role="img" aria-label="Flecha derecha" class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
            </svg>
          </a>
          <a 
            href="#servicios"
            class="inline-flex items-center px-6 py-4 border-2 border-primary-600 text-primary-700 font-semibold rounded-full hover:bg-primary-50 transition-colors duration-300 whitespace-nowrap"
          >
          ¿Cómo puedo ayudarte?
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-8 right-8 lg:right-16">
    <div class="flex flex-col items-center text-primary-600">
      <span class="text-sm font-medium mb-2">Descubre más</span>
      <svg 
        class="w-6 h-6 animate-bounce" 
        fill="none" 
        stroke="currentColor" 
        viewBox="0 0 24 24"
      >
        <path 
          stroke-linecap="round" 
          stroke-linejoin="round" 
          stroke-width="2" 
          d="M19 14l-7 7m0 0l-7-7m7 7V3"
        />
      </svg>
    </div>
  </div>
</section>

<style>
  .animate-bounce {
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%, 100% {
      transform: translateY(-25%);
      animation-timing-function: cubic-bezier(0.8, 0, 1, 1);
    }
    50% {
      transform: translateY(0);
      animation-timing-function: cubic-bezier(0, 0, 0.2, 1);
    }
  }
</style> 