{/* Nuevo diseño del Hero con división diagonal */}
<section id="inicio" class="relative min-h-screen overflow-hidden">
  <div class="absolute inset-0">
    {/* Video de fondo */}
    <div class="absolute inset-0 bg-gray-900/30 z-0 hidden lg:block">
      <video
        class="absolute inset-0 w-full h-full object-cover opacity-80"
        autoplay
        loop
        muted
        playsinline
        preload="auto"
      >
        <source src="/videos/hero.mp4" type="video/mp4" />
      </video>
    </div>
    {/* Fondo rosa para móvil */}
    <div class="absolute inset-0 bg-pink-50/95 lg:hidden"></div>
    {/* Forma diagonal rosa solo para desktop */}
    <div 
      class="absolute top-0 right-0 w-full lg:w-7/12 h-full bg-gradient-to-br from-pink-50 to-pink-100 transform origin-top-left hidden lg:block lg:skew-x-12 lg:translate-x-32 z-10"
      data-aos="fade-left"
    ></div>
  </div>

  <div class="container mx-auto px-4 sm:px-6 relative z-20 h-screen flex items-center">
    <div class="grid grid-cols-12 gap-6 lg:gap-12 items-center w-full">
      {/* Espacio vacío a la izquierda solo en desktop */}
      <div class="hidden lg:block lg:col-span-7"></div>
      
      {/* Contenido centrado en móvil, a la derecha en desktop */}
      <div class="col-span-12 lg:col-span-5 text-center lg:text-left">
        <h1 
          class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-cormorant font-bold mb-4 sm:mb-6 tracking-tight text-primary-950"
          data-aos="fade-up"
          data-aos-delay="300"
          data-aos-duration="1000"
        >
          Viviana Poveda
        </h1>
        <p 
          class="text-lg sm:text-xl md:text-2xl mb-6 sm:mb-8 text-primary-900/90 font-light leading-relaxed"
          data-aos="fade-up"
          data-aos-delay="500"
          data-aos-duration="1000"
        >
          Psicóloga clínica especializada en acompañamiento emocional desde un enfoque integrativo
        </p>
        <div 
          class="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center lg:justify-start"
          data-aos="fade-up"
          data-aos-delay="700"
          data-aos-duration="1000"
        >
          <a
            href="#agenda"
            class="btn btn-primary inline-flex items-center justify-center transform-gpu"
          >
            Agenda tu consulta
            <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </a>
          <a
            href="#servicios"
            class="btn btn-secondary inline-flex items-center justify-center transform-gpu"
          >
            Conoce mis servicios
          </a>
        </div>
      </div>
    </div>
  </div>
</section> 